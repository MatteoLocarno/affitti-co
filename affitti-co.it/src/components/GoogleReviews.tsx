import React from 'react';

const GoogleReviews = ({ placeId, apiKey }: { placeId: string, apiKey: string }) => {
  return (
    <div>
      {/* Qui va il widget delle recensioni Google */}
      <p>Recensioni Google (placeholder)</p>
    </div>
  );
};

export default GoogleReviews; 